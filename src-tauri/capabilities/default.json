{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default permissions for the ReachNote desktop shell.",
  "windows": ["main"],
  "permissions": ["core:default", "core:window:allow-hide"]
}
