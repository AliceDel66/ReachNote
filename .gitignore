.DS_Store
.claude/
.codegraph/

# Rust
/target
/src-tauri/target

# Node
node_modules
dist
dist-ssr
*.local

# Tauri generated
/src-tauri/gen

# Redundant packaged brand assets (extracted files under assets/ are tracked)
/assets/*.zip

# Logs
*.log
npm-debug.log*
pnpm-debug.log*

# Secrets / local credentials — never commit real Notion tokens, API keys, .env values
.env
.env.*
!.env.*.example
*.token
*-secret.*
secrets.*
config/secrets.*
config/*.secret.json
